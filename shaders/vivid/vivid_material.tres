[gd_resource type="ShaderMaterial" load_steps=2 format=3]

[ext_resource type="Shader" path="res://shaders/vivid/vivid_surface.gdshader" id="1_surface"]

; Shader defaults, unmodified, for anything that wants the look without an
; opinion about it — the shader lab duplicates this one per test mesh. Real
; objects should keep their own copy so that base_color and base_texture can
; differ; everything else is worth sharing.

[resource]
resource_name = "VividSurface"
shader = ExtResource("1_surface")
